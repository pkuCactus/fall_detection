# Quick install (CUDA 12.4)
# Recommended: pip install -e ".[torch-cu124,dev]"
# Alternative: pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.5.0+cu124
torchvision>=0.20.0+cu124

# Core dependencies are defined in pyproject.toml
# pip install -e ".[torch-cu124]" will install them automatically
# For development: pip install -e ".[torch-cu124,dev]"